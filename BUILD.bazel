load("@gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/stefanpenner/otel-explorer
gazelle(name = "gazelle")

# Convenience alias so `bazel run //:otel-explorer` works from the repo root.
alias(
    name = "otel-explorer",
    actual = "//cmd/otel-explorer",
)
