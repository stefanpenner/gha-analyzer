load("@gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/stefanpenner/otel-analyzer
gazelle(name = "gazelle")

# Convenience alias so `bazel run //:otel-analyzer` works from the repo root.
alias(
    name = "otel-analyzer",
    actual = "//cmd/otel-analyzer",
)
