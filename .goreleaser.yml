version: 2

builds:
  - id: otel-analyzer
    main: ./cmd/otel-analyzer
    binary: otel-analyzer
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - id: otel-analyzer
    builds: [otel-analyzer]
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    files:
      - none*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  github:
    owner: stefanpenner
    name: otel-analyzer
  draft: false
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

brews:
  - name: otel-analyzer
    repository:
      owner: stefanpenner
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/stefanpenner/otel-analyzer"
    description: "Analyze OpenTelemetry traces to identify performance bottlenecks"
    license: ""
    install: |
      bin.install "otel-analyzer"
    test: |
      system "#{bin}/otel-analyzer", "--help"
